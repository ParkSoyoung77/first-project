01. bastion: 인프라 관리 및 고가용성 설정

1-1. default.conf (Nginx 프록시 설정)
1-2. keepalived.conf (VIP 및 HA 이중화 설정)

=========================================

02. local: 접속 편의성 및 로컬 환경 설정

2-1. config (SSH 점프 프록시 설정)
2-2. hosts (로컬 호스트 파일 설정)

=========================================

03. db-m: 메인 데이터베이스 구성

3-1. init.sql (초기 데이터베이스 구조 및 데이터 설정)
3-2. 마스터-슬레이브 복제.sql

=========================================


04. db-s: 백업 및 복제 자동화 설정
 
4-1. db_backup.sh (백업 실행 스크립트)
4-2. setup_crontab.sh (크론탭 설정 - 정기 백업 스케줄링)
4-3. db-backup-hostpath.yaml (백업용 볼륨 매니페스트)

=========================================

05. k8s-m: 쿠버네티스 프로젝트 디렉토리 (Master)

5-0.1. keepalived.conf (VIP 및 HA 이중화 설정)
5-0.2. ingress-patch.sh (Ingress CofigMap 설정)
--------------------------------------------------

5-1. db-configs: DB 백업 자동화

# 5-1.1. db_backup.sh (DB 백업 실행 스크립트-k8s에 임시 저장)

--------------------------------------------------

5-2. k8s-manifests: 서비스 인프라 매니페스트

< 네트워크 및 인프라 구축 >
5-2.1. metallb.yaml (L4 로드밸런서 VIP 설정)
5-2.2. nginx-conf.yaml (Nginx 설정 ConfigMap)

< 스토리지 설정 >
5-2.3. pv-pvc.yaml (nfs 스토리지 연결)
5-2.4. nfs-log-pv-pvc.yaml (로그 저장용 볼륨)

<데이터베이스 연결>
5-2.5. mariadb-external.yaml (외부 DB 연동 설정)

< 애플리케이션 배포 >
5-2.6. deployment.yaml (Pod 배포 및 복제본 관리)
5-2.7. fastapi.yaml (애플리케이션 상세 설정)
5-2.8. fastapi-services.yaml (내부 서비스 노출)

< 외부 접속 노출 >
5-2.9. ingress.yaml (도메인 기반 라우팅)
5-2.10. grafana-ingress.yaml (모니터링 대시보드 노출)

--------------------------------------------------

5-3. nginx: 정적 웹 리소스

5-3.1. index.html (메인 화면)
5-3.2. 403.html (권한 에러 화면)

--------------------------------------------------

5-4. service-locker: 조회 서비스 (FastAPI)

5-4.1. main.py
5-4.2. Dockerfile
5-4.3. requirements.txt

--------------------------------------------------

5-5. service-locker: 조회 서비스 (FastAPI)

5-5.1. main.py
5-5.2. Dockerfile
5-5.3. requirements.txt

--------------------------------------------------

5-6. service-admin: 관리자 서비스 (FastAPI)

5-6.1. main.py
5-6.2. Dockerfile
5-6.3. requirements.txt

=========================================

06. 장애 대응
 
6-1. disk_extend.sh (하드디스크 용량 늘리기)

